const INPUT_DIRECTORY = './in';
const OUTPUT_DIRECTORY = './out';

// resize section
const resize = true;
const width = 250;

module.exports = {
    INPUT_DIRECTORY,
    OUTPUT_DIRECTORY,
    resize,
    width
}